{
  "name": "orderix-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "build": "npm run build --prefix client",
    "install": "npm install --prefix client"
  }
}
